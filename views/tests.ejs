<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Tests</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .card {
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 20px;
            margin: 20px;
            background-color: #f9f9f9;
            box-shadow: 2px 2px 5px #888888;
        }

        .question {
            font-size: 18px;
            font-weight: bold;
        }

        .createtest{
            width: 10%;
            background: #007BFF;
            color: #fff;
            border: none;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
            cursor: pointer;
        }

    </style>
    <%- include('assets/head') %>
</head>
<header>
    <%- include('assets/header') %>
</header>
<body>
<% if(confirm_message){ %>
<p class="confirm"><%- confirm_message%></p>
<% }%>



<div class="createtest">
<a href="/createTest">Create Test</a>
</div>
<%for (var i = 0; i < all_test.length; i++) {%>
    <div class="card">
        <div class="question"><%= all_test[i].name%></div>
        <div>Minimum point:</div>
        <div class="question"><%= all_test[i].minpoint%></div>
        <a href="/doTest/<%= all_test[i].id %>">Fill</a>
        <%if (current_role==="ROLE_ADMIN" || current_role==="ROLE_TEACHER"){ %>
        <div>
        <a href="/updateTest/<%= all_test[i].id%>">Add Question/Change Name/Delete Test</a>
        </div>
        <% } %>
    </div>
<%}%>

</body>
</html>